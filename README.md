# chess_traps

A new Flutter project.

run buildrunner with ``dart run buildrunner build``
To generate assets use ``asset_gen``
To rename packages use ``dart run package_rename``
To change icons use ``dart run icons_launcher:create``
To build assets use ``dart run asset_gen``
