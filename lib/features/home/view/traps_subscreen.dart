import 'package:flutter/material.dart';

class TrapsSubscreen extends StatelessWidget {
  const TrapsSubscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
