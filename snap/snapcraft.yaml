name: chess_traps
version: 0.1.0
summary: Generated by Icons Launcher
description: Generated by Icons Launcher package for Linux.

confinement: strict
base: core22
grade: stable

slots:
  dbus-chess_traps: # adjust accordingly to your app name
    interface: dbus
    bus: session
    name: org.bar.chess_traps # adjust accordingly to your app name

apps:
  chess_traps:
    command: chess_traps
    extensions: [gnome] # gnome includes the libraries required by flutter
    plugs:
    - network
    slots:
      - dbus-chess_traps
parts:
  chess_traps:
    source: .
    plugin: flutter
    flutter-target: lib/main.dart # The main entry-point file of the application
